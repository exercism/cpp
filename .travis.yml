language: cpp

compiler:
  - gcc
  - clang

before_script:
  - sudo aptitude search libboost
  - sudo apt-get install boost
  - cmake -G "Unix Makefiles"

script:
  - bin/configlet-linux-amd64 .
  - make

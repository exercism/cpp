#pragma once

#include <string>

namespace mary {

    std::string knee_scrached();
    std::string chin_rub();
    std::string raised_shoulder();

    std::string fake_cough();
    std::string foot_tap();
    std::string sock_adjustment();
    
} // namespace mary

